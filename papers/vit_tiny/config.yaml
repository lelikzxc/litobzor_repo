# Tiny Vision Transformer — root config template
# Paper-specific overrides live in configs/config.yaml

model:
  name: vit_tiny
  task: classification

experiment:
  seed: 42
  output_dir: runs/vit_tiny
